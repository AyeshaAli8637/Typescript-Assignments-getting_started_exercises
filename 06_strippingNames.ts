// declaring variable having string with extra space:
let friend = " \t Fiza   "

// printing to display whitespaces around name
console.log(friend);

// printing name with calling trim function on it to remove whitespaces:
console.log(friend.trim());
